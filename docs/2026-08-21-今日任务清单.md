# TK观察工作台 · 2026-08-21 今日任务清单

> 目标：完成通知中心前端收口、明确每日情报中心的可执行设计，并为远程上线验收准备清单。
>
> 协作原则：主线窗口负责 Supabase、migration、RLS、跨工作台数据和部署；本窗口只做独立 UI、文档、测试和不涉及 schema 的模型工作。任何文件被另一窗口修改时停止触碰该文件。

## 今日完成定义

- 通知中心前端具备筛选、分组、未读视觉、空态和偏好入口。
- 每日情报中心有独立产品设计、数据结构和第一期来源边界，不直接承诺不可验证的自动抓取。
- 关键长页面和详情页的导航/抽屉规范得到统一记录。
- 远程上线验收事项可逐条执行，敏感凭据和生产操作不在本窗口直接处理。
- 每个代码改动都有测试或 eval，完成后运行项目门禁并保留提交记录。

## P0：必须优先完成

### 1. 并行窗口协作边界确认

- [ ] 检查主工作区状态和 worktree 列表。
- [ ] 确认另一窗口正在修改的文件，尤其是 `agents.md`、`supabase/migrations/`、`supabase/functions/`、通知数据 hooks。
- [ ] 将本窗口改动限制在独立 UI 文件、测试、eval 和文档。
- [ ] 合并前只查看 diff，不清理或覆盖另一窗口的未提交文件。

### 2. C5 通知中心前端体验收口

责任：主线/并行协作，数据层由另一窗口负责。

- [ ] 通知列表确认筛选：全部、未读、到期、审核、跟进、成交。
- [ ] 确认分组方式：按日期或通知类型分组，避免长列表连续堆叠。
- [ ] 未读状态使用明确的视觉层级，已读内容降低对比度但不隐藏。
- [ ] 空态按筛选条件给出下一步动作，不只显示“暂无数据”。
- [ ] 加载态使用骨架，错误态提供重新加载入口。
- [ ] 详情点击继续支持深链，商机、任务、设计素材能定位到对应页面。
- [ ] 偏好设置只展示当前用户可配置的通知类型，不暴露服务端实现细节。
- [ ] 为筛选、空态、已读状态和偏好组件补 UI 测试/eval。
- [ ] 不修改 provider、Supabase 查询、migration 和 RLS 文件。

### 3. 质量门禁

- [ ] `git diff --check`
- [ ] `pnpm typecheck`
- [ ] `pnpm lint`
- [ ] `pnpm test`
- [ ] `pnpm --dir apps/web test:eval`
- [ ] `pnpm build`
- [ ] 若涉及 Supabase schema，再追加 `pnpm supabase:schema:test` 和 `pnpm supabase:test`。

## P1：今天完成设计和可执行拆分

### 4. 每日情报中心产品设计

- [ ] 定义统一情报对象：标题、摘要、原文链接、来源、抓取时间、地区、语言、主题、热度、可信度、去重键。
- [ ] 定义关联工作台：总览、市场、商务、设计、剪辑、运营。
- [ ] 定义情报状态：未读、已读、收藏、已忽略、已转任务。
- [ ] 定义来源类型：官方公告、RSS、授权数据源、公开页面、人工录入、CSV 导入。
- [ ] 明确不支持没有授权或公开 API 依据的“伪自动抓取”。
- [ ] 设计统一情报池与角色筛选关系，避免每个工作台重复保存同一新闻。
- [ ] 设计首期前端入口：总览摘要 + 独立情报中心 + 工作台关联筛选。
- [ ] 设计转任务字段：目标工作台、负责人、截止时间、来源情报 ID。
- [ ] 增加来源健康度、抓取成功率、去重率、人工采纳率指标。
- [ ] 输出独立设计文档，不直接修改生产 schema。

### 5. 工作台详情和导航规范

- [ ] 盘点总览、商务、市场、设计、剪辑的长页面。
- [ ] 标记需要客户端锚点导航的页面和详情区块。
- [ ] 统一层级选择使用 Cascader 的场景。
- [ ] 统一日期、日期范围和时间选择使用 DatePicker 的场景。
- [ ] 确认 DatePicker 是否允许未来日期，按业务场景区分“历史记录”和“预计日期”。
- [ ] 确认手机端侧栏统一使用汉堡菜单 + Sheet 抽屉。
- [ ] 记录详情抽屉的宽度、滚动区域、固定底部操作和移动端全宽规则。

## P2：有余力再完成

### 6. 前端可见质量改进

- [ ] 为总览、市场、设计、剪辑的核心空态补真实下一步动作。
- [ ] 检查业务表格是否仍存在过度玻璃透明导致的可读性问题。
- [ ] 检查桌面和 390px 手机宽度下的文字溢出、横向滚动和按钮挤压。
- [ ] 检查减少动态效果模式下的页面进入、抽屉和看板反馈。
- [ ] 清理重复的页面工具栏样式，但不进行大规模重构。

### 7. 下一阶段功能拆分

- [ ] 剪辑：视频任务负责人/截止日、成片上传预览、发布排期。
- [ ] 设计：交付文件版本对比、品牌规范数据化、物料自动关联。
- [ ] 市场：真实投放数据、竞品变化时间线和活动复盘对比。
- [ ] 总览：指标筛选、汇率口径、异常解释和跨工作台待办。

## 需要用户确认的外部事项

这些不能由代码提交替代，今天只准备操作清单：

- [ ] 是否关闭公开注册，改为邀请或管理员创建成员。
- [ ] 五个正式角色是否确认当前成员名单。
- [ ] 是否需要 Windows 安装包。
- [ ] 是否允许开始远程 Supabase migration、Secrets、飞书回调和 Edge Function 部署。
- [ ] 是否有正式的 GitHub 远程仓库地址，用于后续 push。
- [ ] 是否允许使用真实业务数据进行线上验收。

## 今日交付物

- [ ] 通知中心 UI 改动和测试提交。
- [ ] 每日情报中心设计文档。
- [ ] 详情页/导航规范补充记录。
- [ ] 今日门禁结果和未解决阻塞项记录。
- [ ] 不提交账号、密码、token、`.env` 或二进制文件。

## 今日不做

- 不直接抓取 TikTok、微信或其他平台的受限数据。
- 不在没有明确授权时执行生产 migration、Secrets、远程部署或删除数据。
- 不修改另一窗口正在处理的通知数据层、migration、Edge Function 和 `agents.md`。
- 不为视觉效果继续叠加无业务意义的光效、粒子或全屏透明卡片。

