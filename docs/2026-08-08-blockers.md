# 未完成任务与阻塞记录

## 飞书知识同步联调

等待本地飞书应用配置：`VITE_FEISHU_APP_ID`、`FEISHU_APP_ID`、`FEISHU_APP_SECRET`、32 位 `FEISHU_TOKEN_ENCRYPTION_KEY` 和 OAuth 回调白名单。密钥不写入仓库、不通过聊天传递。

## Phase D 后端测试

全量 `node --test backend/tests/*.test.cjs` 当前有两个历史测试因模块尚未生成而失败：

- `backend/pb_hooks/lib/dependency-engine.js`
- `backend/pb_hooks/lib/task-engine.js`

本次公众号分析功能的专项测试 3/3 通过，未修改上述范围。
