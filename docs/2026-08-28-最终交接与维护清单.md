# TK观察工作台最终交接与维护清单

更新时间：2026-08-28

## 当前版本

- 当前发布版本：`v0.1.7`
- v0.1.6：`7322c095efc4aceb3391fb0d51fdee23e9744c0b`
- v0.1.7：`00192cf8e07d300b005a02aaefb996a9db365278`
- 当前收口分支：`codex/final-day-closeout`

## 本轮已完成

- 合并每日情报中心设计、工作台交互规范、通知中心、商机详情抽屉和日期选择器等已验证分支。
- 商机、设计任务和发布排期统一支持可选未来日期选择。
- CSV 导入保留 Supabase/PocketBase 双 provider，支持新增、更新、重复跳过的明确反馈，并要求人工确认。
- AI 记录库支持来源/类型筛选、清除筛选、加载失败重试和自定义删除确认；保留 owner-only 决策与审计。
- 全量门禁：前端 164 个测试文件 / 379 条测试通过；`pnpm typecheck`、`pnpm lint`、`pnpm build`、`cargo check`、`git diff --check` 通过。
- `pnpm --dir apps/web format:check` 仍报告 25 个与 `origin/main` 相同的历史格式文件；本轮未改写这些无关基线文件。

## 尚未完成或需要外部条件

- 飞书 OAuth 与同步：需要飞书 App ID、App Secret、正式 HTTPS 回调域名，部署并配置 Supabase Edge Functions 后才能真实验收。
- 微信视频号自动采集：需要用户授权已登录的 Android/ADB/OCR 设备；当前仅有标准化、幂等同步契约，不是微信官方 API。
- Apple Developer ID 签名与 notarization：需要 Apple 开发者账号、证书和公证凭据。
- 远程 Supabase migration/真实业务数据验收：当前 CLI 未 link 生产项目，不能擅自推送或修改生产数据。
- 线上 Cloudflare Pages 与正式成员会话：代码构建已验证，仍需在目标环境完成登录、数据权限和工作流人工验收。

## 换设备恢复

1. 克隆 `https://github.com/Heyehua514/tk-observer.git`，进入项目目录。
2. 安装 Node 20+、pnpm 11+、Rust 和 Tauri 2 所需系统依赖。
3. 执行 `pnpm install --frozen-lockfile`。
4. 在本机创建 `.env`，配置 Supabase URL 和 anon key；不要提交 `.env`、密码、Token、`hosts` 或 service role key。
5. 执行 `pnpm typecheck`、`pnpm lint`、`pnpm test`、`pnpm build`。
6. 需要桌面开发时执行 `pnpm desktop:dev`；发布前使用 GitHub Actions Variables/Secrets，不在本机保存发布秘密。
7. WorkBuddy 需要在新设备重新登录本机客户端；首次使用前确认本机网关健康检查和权限提示。

## 发布与验收

- 发布前确认工作区无敏感未跟踪文件，运行完整门禁并检查 `git diff --check`。
- 通过分支 PR 合并到 `main` 后创建版本 tag，观察 GitHub Actions 完成，再从 Release 下载对应平台安装包。
- 安装后验证登录、Supabase 数据读取、AI 助手主动调用、AI 记忆保存/删除、通知和核心工作台页面。
- 真实数据采集、飞书授权、生产 migration 和 Apple 公证均需在具备对应外部权限后单独验收。

## 安全边界

- 不读取、不提交、不复制密码文件、Token、二维码、微信登录凭据、设备序列号或 `hosts`。
- AI 只读取当前用户有权限的数据，并在用户主动点击后分析；不会自动修改业务数据。
- 任何真实生产变更先完成备份、权限确认和回滚演练。
