# 真实业务数据验收清单

> 适用：本地 Supabase 运行环境（`pnpm supabase:start`）与真实业务数据录入验收。
> 前置：`pnpm supabase:reset && pnpm supabase:test && pnpm supabase:types` 全过；`pnpm dev` 启动 Web。
> 2026-08-13 深夜：B1-B7 自动验收记录已勾选（标注「自动覆盖」项）；涉及真机/交互的项留待真机验收。

## 0. 启动与账号

```bash
pnpm supabase:start
pnpm dev
```

使用已注册的五角色账号登录（磊哥 boss / 董雨辰 business / 韩素云 market / 孙铭泽 design / 谢洁 editing）。测试成员杨振康账号用于 business 并发验证；密码见本地会话记录，不写入仓库。

## 1. 市场工作台（韩素云）

- [x] 新建活动「金鳞会·厦门闭门沙龙」，自动生成 7 条财务模板（自动覆盖：pgTAP acceptance_full_loop 断言 7 条模板，金额 0；E2E `market-events.spec.ts` 走通活动新增-列表-删除）。
- [x] 为活动补 4 个阶段与任务，阶段进度自动计算（自动覆盖：pgTAP event_collaboration / acceptance_full_loop）；看板拖拽交互待真机。
- [x] 活动详情 6 个 Tab 可切换：概览甘特、任务看板、招商跟进、报名管理、财务复盘、进度总览（自动覆盖：E2E `market-events.spec.ts` 详情用例逐 Tab 切换并断言渲染；财务导出按钮可见）。
- [ ] 上传场地照片（多图），验证 Storage 私有访问与缩略图（storage_access pgTAP 覆盖私有访问/缩略图策略；上传交互待真机）。
- [ ] 录入场地、文案模板、物料与财务明细，验证筛选/搜索/导出（数据层由业务/脚本测试覆盖；真实录入与 Markdown+CSV 导出交互待真机）。

## 2. 商务工作台（董雨辰）

- [x] 客户 CRUD 闭环：登录 → 新增 → 检索命中 → 删除回收（自动覆盖：Playwright E2E `e2e/business-crud.spec.ts`）；行业/来源/重要度筛选交互待真机。
- [ ] 商机 Pipeline：阶段流转、「已成交」自动通知磊哥、「已流失」必填原因并沉淀 failed_cases（自动覆盖：pgTAP business_transactions）；6 列看板拖拽交互待真机。
- [ ] 渠道商单：仅可选择 `is_biz_available=true` 的达人；状态流转与取消原因必填（RLS/达人商务字段由 pgTAP 覆盖；表单交互待真机）。
- [ ] 朋友圈计划：日历视图建计划、发布后回填结果与转化商机，商机 notes 自动追加来源（数据层由 pgTAP 覆盖；日历交互待真机）。
- [x] 公众号分析第 9 Tab：文章录入后 is_viral 由数据库自动计算（自动覆盖：pgTAP blog_articles.test.sql 断言自动计算与权限）。

## 3. 设计工作台（孙铭泽）

- [x] 接收商务/老板提交的设计需求，接单（pending→in_progress）→交付（delivered）（自动覆盖：pgTAP design_workspace + E2E `design-request-flow.spec.ts` 磊哥提交 → 孙铭泽接单闭环；交付交互待真机）。
- [ ] 素材上传后提交审核；磊哥通过或驳回（驳回理由必填），通知到孙铭泽（审核状态机与通知由 pgTAP 覆盖；驳回弹窗交互待真机）。
- [ ] 需求详情页展示参考图与交付记录子 Tab（数据关联由 pgTAP 覆盖；详情页子 Tab 交互待真机，截图覆盖设计页渲染）。

## 4. 剪辑工作台（谢洁）

- [x] 选题 CRUD + CSV 导入/导出；is_viral 由数据库自动计算（自动覆盖：pgTAP video_viral_engine.eval + 脚本测试覆盖 CSV 导入导出与列归一化 + E2E `editing-video-idea.spec.ts` 新增-列表-删除）。
- [ ] 对标账号/视频、热点话题、风格分析数据可录入与查询（数据层由 pgTAP editing_research_records 覆盖；录入交互待真机）。
- [x] 视频任务与成片归档骨架可进入，空态有引导文案（自动覆盖：pgTAP editing_production + UI 测试空态文案 + 2026-08-13 截图渲染）。

## 5. 总览工作台（磊哥）

- [x] 指标卡、GMV 趋势与团队动态展示（自动覆盖：pgTAP overview_dashboard + UI 测试 + 截图）；count-up 动效待真机预览。
- [x] 今日简报、本月教训、闭环仪表（团队记忆）读取 Supabase daily_reports / failed_cases（自动覆盖：pgTAP team_memory + team_memory_automation；截图渲染总览页）。
- [x] 通知铃铛：审批、商机成交、到期提醒（自动覆盖：pgTAP notifications + UI 测试）；实时到达待真机。
- [ ] 团队日历聚合活动、任务、设计需求、朋友圈计划与商单排期（跨表聚合由 acceptance_full_loop 部分覆盖；日历 UI 待真机）。

## 6. 权限矩阵（越权验证）

- [x] business 请求 market 专属数据（venues 等）被 RLS 拒绝（自动覆盖：pgTAP market_business_master_data / market_resources 越权断言）。
- [x] design 请求 business 客户数据被拒（自动覆盖：pgTAP RLS 越权断言）。
- [x] editing 请求总览 GMV 数据被拒（自动覆盖：pgTAP overview_dashboard RLS 断言）。
- [x] 未登录直接访问工作台路由被重定向登录页（自动覆盖：Playwright E2E 登录 + 路由守卫 UI 测试）。

## 7. 数据迁移核对（只读）

```bash
node scripts/supabase/reconcile-pb-export.mjs
node scripts/supabase/export-pb-business.mjs
```

- [x] 对账输出：无列缺口（2026-08-13 实跑：PB 44 表 / Supabase 39 表，12 张映射表有数据，PB-only 7 张均为空或配置表；报告 /tmp/tk-observer-supabase/reconcile-report.json）。
- [x] 导出 JSON/CSV 可打开，列名已归一化到 Supabase 目标列（自动覆盖：export-pb-business 脚本测试 + 实跑导出）。

## 8. 门禁（每次验收结束固定跑）

```bash
git diff --check
pnpm typecheck
pnpm lint
pnpm test
pnpm supabase:schema:test
pnpm supabase:test
```

- 2026-08-13 深夜实跑：git diff --check ✅；typecheck ✅；lint ✅（修复 vite.config.ts 三重斜杠引用）；pnpm test 99 文件 / 224 测试 ✅；supabase:schema:test ✅；supabase:test 23 文件 / 401 断言 Result: PASS ✅。

## 9. 已知待办（阻塞项，需运行环境）

- 图片/视频/设计文件迁移到 Supabase Storage 并抽样验证 URL 权限与缩略图（storage 策略已就绪并测试，文件本体迁移待执行）。
- 真实业务数据：12 张业务表已导入本地测试库，6 个验收账号已写入；剩余表按需增量导入（import-business-data.mjs）。
- 移动端真机访问验证与 PWA 落地（manifest / service worker / 离线壳已就绪，真机待验）。
- 回滚演练：脚本与手册已就绪（docs/pocketbase-rollback-drill.md），未执行真实回切。
- 远程部署执行：Supabase 生产项目 + 静态托管 + 域名（待用户决策）。
