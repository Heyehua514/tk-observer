# 真实业务数据验收清单

> 适用：本地 Supabase 运行环境（`pnpm supabase:start`）与真实业务数据录入验收。
> 前置：`pnpm supabase:reset && pnpm supabase:test && pnpm supabase:types` 全过；`pnpm dev` 启动 Web。
> 2026-08-13 深夜：B1-B7 自动验收记录已勾选（标注「自动覆盖」项）；涉及真机/交互的项留待真机验收。

## 0. 启动与账号

```bash
pnpm supabase:start
pnpm dev
```

使用已注册的五角色账号登录（磊哥 boss / 董雨辰 business / 韩素云 market / 孙铭泽 design / 谢洁 editing）。测试成员杨振康账号用于 business 并发验证；密码见本地会话记录，不写入仓库。

## 1. 市场工作台（韩素云）

- [x] 新建活动「金鳞会·厦门闭门沙龙」，自动生成 7 条财务模板（自动覆盖：pgTAP acceptance_full_loop 断言 7 条模板，金额 0；E2E `market-events.spec.ts` 走通活动新增-列表-删除）。
- [x] 为活动补 4 个阶段与任务，阶段进度自动计算（自动覆盖：pgTAP event_collaboration / acceptance_full_loop + E2E `activity-task-board.spec.ts` REST 注入 P0 阶段与任务 → 看板拖拽（待处理→进行中）→ 状态列实时更新全闭环）。
- [x] 活动详情 6 个 Tab 可切换：概览甘特、任务看板、招商跟进、报名管理、财务复盘、进度总览（自动覆盖：E2E `market-events.spec.ts` 详情用例逐 Tab 切换并断言渲染；财务导出按钮可见）。
- [x] 上传场地照片（多图），验证 Storage 私有访问与缩略图（自动覆盖：storage_access pgTAP 覆盖私有访问/缩略图策略 + E2E `venue-resource.spec.ts` 多图上传与详情轮播切换）。
- [x] 录入场地、文案模板、物料与财务明细，验证筛选/搜索/导出（自动覆盖：E2E `market-resources.spec.ts` 文案模板录入+占位符预览+套用计数、物料上传（文件存储）与预览、财务明细录入+CSV/Markdown 导出（下载内容断言）+ E2E `venue-filter.spec.ts` 场地名称/标签搜索、城市/类型筛选与重置 + E2E `venue-quick-match.spec.ts` 快速匹配按城市/活动人数/场地类型组合筛选与收起回收）。

## 2. 商务工作台（董雨辰）

- [x] 客户 CRUD 闭环：登录 → 新增 → 检索命中 → 删除回收（自动覆盖：Playwright E2E `e2e/business-crud.spec.ts`）；行业/来源/重要度筛选与名称/对接人搜索由 E2E `client-filter.spec.ts` 自动覆盖。
- [x] 客户详情关联面板：关联商机/商单条目点击跳转，直达对应工作台并自动打开详情/定位高亮（自动覆盖：E2E `client-relation-jump.spec.ts` 建客户+商机+商单 → 详情点「关联商机」→ 跳 Pipeline 自动开详情 → Esc 关闭 → 点「关联商单」→ 跳渠道商单断言高亮 → 软删回收；UI 测试 `relation-block.test.tsx` 覆盖点击回调与空态）。
- [x] 商机 Pipeline：阶段流转、看板拖拽、「已成交」自动通知磊哥、「已流失」必填原因并沉淀 failed_cases（自动覆盖：pgTAP business_transactions + E2E `opportunity-pipeline.spec.ts` 拖拽与流失必填闭环）。
- [x] 达人商务标记：可商务合作筛选、列表/详情只读展示、商单表单仅列可商务达人（自动覆盖：E2E `creator-biz-mark.spec.ts` 谢洁建达人 → 董雨辰标记 → 商单下拉只列可商务达人；RLS 由 pgTAP 覆盖）。
- [x] 渠道商单：状态流转与取消原因必填（自动覆盖：E2E `order-status.spec.ts` + pgTAP channel_order_cancel_reason）。
- [x] 朋友圈计划：日历视图建计划、列表视图状态流转（已计划→已发布→已复盘）、删除回收（自动覆盖：pgTAP + E2E `social-plan.spec.ts`）；发布后复盘弹窗回填实际效果与转化商机，触发器自动在商机 notes 追加「来源：朋友圈 M月D日 内容」（同一行不重复、第二条朋友圈追加新行，自动覆盖：pgTAP `team_memory_automation` 5 断言 + E2E `social-plan-review.spec.ts` 完整交互闭环）。
- [x] 公众号分析第 9 Tab：文章录入后 is_viral 由数据库自动计算（自动覆盖：pgTAP blog_articles.test.sql 断言自动计算与权限）。

## 3. 设计工作台（孙铭泽）

- [x] 接收商务/老板提交的设计需求，接单（pending→in_progress）→交付（delivered）（自动覆盖：pgTAP design_workspace + E2E `design-request-flow.spec.ts` 磊哥提交 → 孙铭泽接单闭环；交付交互待真机）。
- [x] 设计任务四列看板（待设计/进行中/待审核/已完成）：新增任务落待设计列，拖拽流转状态实时更新（自动覆盖：E2E `design-task-board.spec.ts` 新增 → 拖拽待设计→进行中 → 状态列断言 → 软删回收）。
- [x] 素材上传后提交审核；磊哥通过或驳回（驳回理由必填），通知到孙铭泽（自动覆盖：pgTAP design_workspace 审核状态机与通知断言 + E2E `design-review-loop.spec.ts` 上传→提审→驳回（空理由拦截）→再提审→通过全闭环）。
- [x] 需求详情页展示参考图与交付记录子 Tab（自动覆盖：pgTAP design_workspace 关联约束 + E2E `design-requirement-subtabs.spec.ts` 素材过审 → 需求 → 孙铭泽详情加参考与交付 → 磊哥只读看到交付记录、无编辑表单全闭环）。

## 4. 剪辑工作台（谢洁）

- [x] 选题 CRUD + CSV 导入/导出；is_viral 由数据库自动计算（自动覆盖：pgTAP video_viral_engine.eval + 脚本测试覆盖 CSV 导入导出与列归一化 + E2E `editing-video-idea.spec.ts` 新增-列表-删除）。
- [x] 对标账号/视频、热点话题、风格分析数据可录入与查询（自动覆盖：pgTAP editing_research_records 数据层断言 + E2E `editing-research-entry.spec.ts` 对标视频录入/分析笔记编辑、风格分析粘贴解析、热点话题批量解析+转为选题全闭环）。
- [x] 视频任务与成片归档骨架可进入，空态有引导文案（自动覆盖：pgTAP editing_production + UI 测试空态文案 + 2026-08-13 截图渲染）。

## 5. 总览工作台（磊哥）

- [x] 指标卡、GMV 趋势与团队动态展示（自动覆盖：pgTAP overview_dashboard + UI 测试 + 截图）；count-up 动效待真机预览。
- [x] GMV 支持近 7 天、近 30 天、全部范围筛选，指标与曲线同口径；金额统一按人民币元展示、分存储（自动覆盖：`overview-metrics.test.ts` + 总览 UI 测试）。
- [x] 近期活动展示未来最近三场，点击直达市场活动详情；活动阶段分布图按共享 events 数据聚合（自动覆盖：活动筛选与状态图模型测试 + 总览 UI 空态测试）。
- [x] 今日简报、本月教训、闭环仪表（团队记忆）读取 Supabase daily_reports / failed_cases（自动覆盖：pgTAP team_memory + team_memory_automation；截图渲染总览页）。
- [x] 通知铃铛：审批、商机成交、到期提醒（自动覆盖：pgTAP notifications + UI 测试）；实时到达（自动覆盖：E2E `notification-realtime.spec.ts` 双上下文：董雨辰成交商机 → 磊哥总览铃铛不刷新弹出通知）。
- [x] 团队日历聚合活动、任务、设计需求、朋友圈计划与商单排期（跨工作台 E2E `team-calendar.spec.ts` 12/12 全过；查询键稳定性修复，时钟重渲染不再请求风暴）。

## 6. 权限矩阵（越权验证）

- [x] business 请求 market 专属数据（venues 等）被 RLS 拒绝（自动覆盖：pgTAP market_business_master_data / market_resources 越权断言）。
- [x] design 请求 business 客户数据被拒（自动覆盖：pgTAP RLS 越权断言）。
- [x] editing 请求总览 GMV 数据被拒（自动覆盖：pgTAP overview_dashboard RLS 断言）。
- [x] 未登录直接访问工作台路由被重定向登录页（自动覆盖：Playwright E2E 登录 + 路由守卫 UI 测试）。

## 7. 数据迁移核对（只读）

```bash
node scripts/supabase/reconcile-pb-export.mjs
node scripts/supabase/export-pb-business.mjs
```

- [x] 对账输出：无列缺口（2026-08-13 实跑：PB 44 表 / Supabase 39 表，12 张映射表有数据，PB-only 7 张均为空或配置表；报告 /tmp/tk-observer-supabase/reconcile-report.json）。
- [x] 导出 JSON/CSV 可打开，列名已归一化到 Supabase 目标列（自动覆盖：export-pb-business 脚本测试 + 实跑导出）。

## 8. 门禁（每次验收结束固定跑）

```bash
git diff --check
pnpm typecheck
pnpm lint
pnpm test
pnpm supabase:schema:test
pnpm supabase:test
```

- 2026-08-13 深夜实跑：git diff --check ✅；typecheck ✅；lint ✅（修复 vite.config.ts 三重斜杠引用）；pnpm test 99 文件 / 224 测试 ✅；supabase:schema:test ✅；supabase:test 23 文件 / 401 断言 Result: PASS ✅。
- 2026-08-14 下午实跑：git diff --check ✅；typecheck ✅；lint ✅；pnpm test 102 文件 / 237 测试 ✅；pnpm --dir apps/web build ✅；全量 E2E 22/22 全过（1.2m，新增活动任务看板拖拽用例）；残留验证 events / event_tasks / event_phases `E2E任务看板%` live=0 ✅。
- 2026-08-14 下午二跑：typecheck ✅；lint ✅；pnpm test 102 文件 / 237 测试 ✅；pnpm --dir apps/web build ✅；全量 E2E 23/23 全过（1.2m，新增设计任务看板拖拽用例）；残留验证 design_tasks `E2E设计任务%` live=0 ✅。
- 2026-08-14 下午三跑：typecheck ✅；lint ✅；pnpm test 102 文件 / 237 测试 ✅；pnpm --dir apps/web build ✅；全量 E2E 24/24 全过（1.3m，新增场地快速匹配组合筛选用例，单跑 2 条场地用例 11.4s）；残留验证 venues `E2E匹配场地%` / `E2E筛选场地%` live=0 ✅。
- 2026-08-14 下午四跑：typecheck ✅；lint ✅；pnpm test 103 文件 / 240 测试 ✅；pnpm --dir apps/web build ✅；全量 E2E 25/25 全过（1.4m，新增客户详情关联面板跳转用例，单跑 6.9s）；残留验证 clients / opportunities / channel_orders `E2E关联%` live=0 ✅。
- 2026-08-14 下午五跑：typecheck ✅；lint ✅；pnpm test 103 文件 / 240 测试 ✅；pnpm build ✅；supabase test db 26 文件 / 469 断言 PASS（新增商机成交通知 12 断言）；全量 E2E 26/26 全过（1.4m，新增铃铛 Realtime 到达双上下文用例，单跑 4.1s）；残留验证 notifications / opportunities / clients `E2E成交%` live=0 ✅；文件迁移 `node --test scripts/supabase/*.test.mjs` 44/44 + migrate-files --verify FILE_VERIFY_PASSED（活跃路径 2、缺对象 0、MIME 不符 1 已知例外）+ 角色令牌抽样（design 读 design-assets 200，editing/business 拒绝 400）。
- 2026-08-14 下午六跑：typecheck（tsc -b --force）✅；lint ✅；pnpm test 103 文件 / 240 测试 ✅；全量 E2E 30/30 全过（1.5m，新增 PWA 移动端外壳 4 条用例：manifest/SW 注册接管/离线壳回退/iPhone 视口无溢出，均无需登录）。
- 2026-08-14 下午七跑：回退演练 `--drill` DRILL_READY_PASS（data.db 843776 字节 / migration 21 个 / 导出目录 15 个文件）；`node --test scripts/supabase/*.test.mjs` 46/46（新增 evaluateReadiness 2 断言）；typecheck/lint 不受脚本改动影响保持零错误。

## 9. 已知待办（阻塞项，需运行环境）

- [x] 图片/视频/设计文件迁移到 Supabase Storage 并抽样验证：migrate-files.mjs 已实跑（design-assets 上传 1 个真实 PB 文件，幂等跳过已存在对象，MIME 不符 1 条记录在案）；--verify 一致性 0 缺口；角色令牌抽样 URL 权限（design 200 / editing·business 400 拒绝）；缩略图沿用签名 URL 原图渲染（本地图片变换关闭，生产可开启）。
- 真实业务数据：12 张业务表已导入本地测试库；远程库尚无业务数据，按需通过 `import-business-data.mjs` 或人工录入。
- [x] PWA 三要素与移动端视口自动化验证：E2E `pwa-mobile-shell.spec.ts` 4 条用例（manifest 元数据与图标可加载、SW 注册并被接管、离线外壳回退渲染登录页、iPhone 13 视口无横向溢出）；真机点验仍保留为上线前人工项。
- [x] 回滚演练 dry-run 就绪判定：`pocketbase-rollback.mjs --drill` 输出 DRILL_READY_PASS（data.db 843776 字节、migration 21 个 ≥ 15、导出目录 15 个文件）并以退出码门禁；真实回切（provider 切换 + 启动 PocketBase + 前端抽查）为人工步骤待用户执行。
- [x] 远程 Supabase 生产项目：`mddldiwthvnaefwtlnme` 已创建并推送全部 36 个 migration；6 个成员账号已创建并通过登录与角色访问验证（2026-08-18）。
- 远程 Web 静态托管 + 域名：待用户提供托管平台账号与域名（下一步）。
- Windows 安装包：需要 Windows 构建环境（待办）。
- [x] 全仓 Prettier 格式基线：`apps/web/src` 历史 74 个文件已单独格式化并通过全量质量门禁。
- [x] 全量业务 E2E：31 个 Playwright 用例全部通过；密码仅通过当前进程的 `TK_OBSERVER_TEST_PASSWORD` 注入，不入库、不入文档。

## 10. 前端体验质量收尾（2026-08-20 部分）

> 纯前端质量收尾，已并入 main（提交 20bfc17 等）。均为简单项；复杂项 C2-C5 由另一窗口在 `main` 推进，本清单不记录其进度。

- [x] 原生浏览器弹窗全部替换：商机拖入「已流失」/商务驾驶舱快捷流失改自定义 Dialog，删除发布排期改 AlertDialog（`window.prompt/confirm` 已清零）。
- [x] 总览「成员任务进度」下真数据：删掉写死的五人名单与 `[82,68,54,76,61]` 假进度，只渲染 `team_tasks` 真实记录，空数据给引导文案。
- [x] 市场「竞品监测」摘要卡下真数据：去掉写死 3 个公众号，改读 `competitor_accounts`（`MarketCompetitorSummary`），空数据显示「暂无监测账号」。
- [x] 加载态统一：`TableSkeleton` + `LoadStateError`（含「重新加载」）覆盖达人/公司/场地/选题/对标/趋势/审批/竞品/通知/视频任务等列表，消除「正在加载空白闪现为暂无数据」的体验。
- [x] 通知深链：商机/活动任务截止通知点击直达记录（商机开详情对话框、活动任务高亮并滚动）——迁移 `20260814001000`。
- [x] 商务工作台 9 Tab 分组：低频项（客户/供应商、活动招商、公众号分析）收进「更多」下拉，主栏保留 6 个高频项。
- [x] 侧边栏「最近访问」：sessionStorage 记录最近 5 个一级工作台，退出登录即清空。
- [x] 总览指标卡点击跳转：「本周出片数」卡点击进剪辑工作台。
- [x] 全局搜索「查看全部」可点：死文案改为跳 `/search?q=&kind=` 结果页。
- [x] 剩余加载文本改骨架：design 素材网格、活动详情加载态用 pulse 骨架。
- [x] C6 移动端侧边栏抽屉验证：`sidebar.tsx`（`useIsMobile` + `Sheet`）原生已支持；新增 `mobile-sidebar-drawer.spec.ts` 断言 iPhone 视口可拉出抽屉、跳转并关闭（需 Supabase+密码真跑）。
