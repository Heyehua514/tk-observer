# 2026-08-20 开发日志

## 完成

- 新增设计稿提交审核后的老板通知，含 `/design` 深链与重复提审去重。
- 新增 Supabase-first 飞书基础数据层：连接、文档、同步游标三张表。
- 飞书连接状态前端改为 Supabase RPC 优先读取，浏览器不读取 access token 或 refresh token 字段。

## 可量化验证

- 设计审核通知：Supabase 31 个测试文件 / 508 条断言通过。
- 飞书基础：2 个专项测试文件 / 18 条断言通过。
- 前端飞书连接映射：2 条专项测试通过。

## 经验与反思

- pgTAP `plan()` 必须和实际断言数逐条匹配。本日两次发现计划数偏差，均在全量门禁前修正。
- RLS 策略不等于数据库权限。文档读取需要同时具备 `SELECT` 授权与 owner RLS；连接 token 表必须同时取消表级读取和 RLS 读取路径。
- 已应用 migration 不能回改。权限修正必须追加 `20260820000410_feishu_document_read_grants.sql`，不能修改已应用的 `20260820000400`。
- Edge Function 缺少 Deno 本地运行环境时，只完成可本地证实的数据层和前端契约，不把 OAuth 交换标记为可用。

## 风险与阻塞

- 飞书 OAuth Edge Function、定时同步和真实飞书授权仍未完成。
- 阻塞条件：本机未安装 Deno；飞书 App ID、App Secret、正式 HTTPS 回调地址尚未配置到 Supabase Edge Function Secrets。
- 远程 Supabase 未应用本日新增 migrations，生产数据未发生变更。

## 下一节点

1. 安装或提供 Deno Edge Function 本地验证环境。
2. 实现 `feishu-oauth` Edge Function，先覆盖无密钥拒绝和 token 不返回浏览器的测试。
3. 配置飞书开放平台回调地址与 Supabase Secrets 后，单独申请生产验证。
