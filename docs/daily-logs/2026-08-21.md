# 2026-08-21 开发日志

## 完成

- 远程 Supabase 项目 `mddldiwthvnaefwtlnme` 已应用新增 migration `20260821000100_search_performance.sql`。
- 搜索性能切片上线：`pg_trgm`、七组未软删除数据 GIN 索引、全局搜索 250ms 防抖。

## 验证

- `supabase migration list`：本地与远程均包含 `20260821000100`。
- 远程推送未修改历史 migration、未写入 Secrets、未部署 Edge Function。

## 风险与反思

- `supabase db push --debug` 在远程 catalog 缓存阶段出现 pg-delta 超时警告，但命令最终返回成功；随后 `migration list` 已确认远程版本号一致。
- 后续涉及查询计划的性能验证应在远程低峰期执行 `EXPLAIN`，不在生产写入测试数据。

## 客户全局搜索修复

- 根因：商务客户页使用 `clients`，全局搜索 Supabase 分支仍查询旧 `companies` 表，导致列表有数据但顶部搜索为空。
- 修复：全局客户搜索改查 `clients`，覆盖 `name`、`contact_name`、`company`；映射兼容新旧字段。
- 新增 gate/eval 覆盖 clients 行映射和客户名称展示，专项测试 132 文件 / 304 测试通过。
- 完整门禁通过：typecheck、lint、test、build、git diff --check。
- Cloudflare Pages 已部署：`https://59a1683c.tk-observer.pages.dev`，`/login` 返回 HTTP 200；正式域名刷新后即可验收。

## 反思

- 本次问题不是数据库索引或 RLS，而是 Supabase 查询表名未随业务表迁移同步，说明全局搜索需要和各工作台数据源建立表级契约测试。
- 当前已用 clients 行结构测试阻止回退到旧 companies 字段；后续新增业务表时应同时更新搜索映射与 eval，避免只验证列表页而遗漏跨模块入口。

## 通知偏好失败态修复

- 根因：通知偏好 hook 已返回查询错误，但页面只判断加载中或数据是否存在，查询失败时一直显示转圈。
- 修复：接入现有 `LoadStateError`，展示中文错误说明和重新加载入口；hook 暴露 `isError` 与 `refetch`。
- 新增 UI 回归：`notification-preferences.test.tsx`，验证读取失败时错误态和重试按钮可见。
- 验证：前端完整门禁 133 个测试文件 / 305 个测试通过，typecheck、lint、build、git diff --check 全部通过。
- Cloudflare Pages 已部署：`https://bf2f0b83.tk-observer.pages.dev`，`/login` 返回 HTTP 200。

## WorkBuddy 网关开机自启

- 新增 macOS `launchd` 配置和一键安装脚本：`scripts/workbuddy-gateway/com.tkobserver.workbuddy-gateway.plist`、`install-launch-agent.command`。
- 安装后登录系统自动启动，异常退出自动拉起；仍只监听 `127.0.0.1:8877`，不暴露公网。
- 已在本机安装并验证 `launchctl` 状态为 running，健康检查返回 `{"ok":true,"workbuddy":"ready"}`。
- 完整前端门禁：133 个测试文件 / 305 个测试，typecheck、lint、build、git diff --check 全部通过。

## 个人 AI 助手与记忆第一阶段

- 新增角色配置：经营、商务、活动、设计、内容五类专属助手名称和重点，复用同一 AI 面板。
- 新增任务上下文纯函数：只保留标题、状态、截止日期、备注和来源，最多 12 条，优先逾期/待处理，自动剔除密码/token 等敏感字段。
- 新增追加 migration `20260821000300_ai_memory.sql`：个人记忆 owner-scoped RLS、唯一记忆键、软删除和 Realtime；前端 hook 支持本人查询、确认保存和软删除。
- AI 面板调用时读取本人已确认记忆，结果支持人工点击「记住这条」，不会自动写入。
- 验证：前端 135 文件 / 308 测试通过，typecheck、lint、build、schema inventory 2/2、git diff --check 全部通过。
- 远程 Supabase 未推送新 migration，需生产验收后再执行。

## 个人 AI 第二阶段：记忆管理、网关统一与推进建议

- 修复 `AiMemoryView` 浏览器测试 mock 路径：组件与测试统一使用 `@/features/shared-ai/hooks/use-ai-memory`，个人记忆列表和删除行为专项通过。
- 新增 `workbuddy-gateway` 共享调用层，统一 AI 面板、视频 AI hook、视频 AI 面板的本机网关地址、错误处理和 JSON 响应校验；移除视频 AI hook 的占位返回。
- 总览成员任务增加 Sparkles 分析入口，点击后把脱敏任务摘要填入总览专属 AI，仍由用户手动发起和确认，不自动修改任务。
- 全局搜索新增只读“推进建议”结果模型，按达人、客户、视频结果给出下一步入口提示，不写入业务数据。
- 验证：前端 137 个测试文件 / 312 个测试通过，typecheck、lint、build、git diff --check 通过。本轮改动文件已单独通过 Prettier；仓库仍有 16 个历史格式警告，未扩大范围修复。

### 反思

- 个人 AI 的真实价值在“上下文 + 人工确认 + 可追溯记忆”，因此继续保持 AI 只读建议，避免把模型输出直接变成业务状态变更。
- 网关调用集中后，后续新增 AI 功能只需复用一个错误契约；视频 AI 解析也复用了现有 JSON parser，减少格式漂移。
- 搜索推进建议先做确定性规则，避免把每次搜索都变成 WorkBuddy 消耗；只有用户点击 AI 分析时才调用本机网关。

## 个人 AI 第二阶段预览发布

- Cloudflare Pages 已部署：`https://31c1d562.tk-observer.pages.dev`。
- 只读可达性核验：`/login` 返回 HTTP 200。
- 未推送 Supabase migration，线上数据库结构保持不变；该预览中记忆管理需等 `ai_memory` migration 获得生产确认后才可实际写入。
