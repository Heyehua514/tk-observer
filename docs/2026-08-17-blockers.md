# 2026-08-17 门禁阻塞记录

## 节点 14：全仓 Prettier 基线

- `pnpm --dir apps/web exec prettier --check src` 报告 75 个历史文件未按当前 Prettier 格式化。
- 这些文件在本轮开始前已经提交，当前工作树没有对应修改。
- 批量格式化会引入与功能无关的大范围 diff，因此没有执行。
- 本轮涉及的 20 个文件已单独通过 Prettier 检查；typecheck、lint、前端测试、Supabase schema 测试和数据库测试均通过。
- 后续建议：单独安排格式化基线变更，避免与业务功能混合提交。

## 节点 15：全量 E2E 业务登录凭据

- Playwright 共发现 31 个用例，4 个 PWA/移动端用例通过。
- 27 个业务闭环用例因未设置 `TK_OBSERVER_TEST_PASSWORD` 按测试文件的安全守卫跳过。
- 密码保持在环境变量中，不写入仓库；周一配置本地测试账号后重跑 `pnpm --dir apps/web test:e2e`。
