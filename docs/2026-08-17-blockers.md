# 2026-08-17 门禁阻塞记录

## 节点 14：全仓 Prettier 基线（已解决）

- 已对 `apps/web/src` 的 74 个历史文件执行独立的纯格式化提交，未混入业务逻辑修改。
- 复验通过：`prettier --check src`、typecheck、lint、前端测试、构建、Supabase schema 测试及数据库测试。

## 节点 15：全量 E2E 业务登录凭据

- Playwright 共发现 31 个用例，4 个 PWA/移动端用例通过。
- 27 个业务闭环用例因未设置 `TK_OBSERVER_TEST_PASSWORD` 按测试文件的安全守卫跳过。
- 密码保持在环境变量中，不写入仓库；周一配置本地测试账号后重跑 `pnpm --dir apps/web test:e2e`。
