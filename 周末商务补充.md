# 商务工作台 · Weekend 自主执行模式

> 用户周末不在线。你独立完成商务工作台公众号分析功能。遇到问题自检自修，遇到阻塞记录并继续下一步。不能因为小问题停滞。

---

## 安全红线（碰一条自动失败，不碰就继续推进）

**禁止：**
- 不改已发布的 15 个 migration 文件（1785859200-1786001100）
- 不引入 Express/NestJS/Django/Redux/MobX
- 不删已有表字段、不删已有数据
- 不执行 `rm -rf`、`DROP TABLE`、`git push --force`
- 不访问外部 API（不做 fetch/curl 到公网）
- 不启动服务（不做 pocketbase serve、npm run dev）
- 不改已有 API Rules（只新增，不删不改旧规则）

**允许：**
- 新建 migration 文件
- 新建组件/hooks/测试文件
- 跑 typecheck/lint/test
- 跑 git status / git diff / git add / git commit
- 改新文件反复调试直到通过
- 出错了改新文件重新跑

---

## 自检循环（每步失败自动重试3次，3次都不过写原因跳到下一步）

```
做一步
  ├→ 跑对应检查
  │    ├→ 过 → 进入下一步
  │    └→ 挂 → 读错误信息 → 改新文件修复 → 重试（最多3次）
  │              └→ 3次都不过 → 写入 docs/2026-08-08-blockers.md → 跳到下一步
  └→ 继续
```

**每步对应的检查命令：**

| 步骤 | 自检命令 | 失败时 |
|---|---|---|
| 建 migration | 无（migration 本身不跑） | 语法检查：`node -c 文件名` |
| 写组件 | `pnpm typecheck` | 读 TS 错误→改新文件 |
| 写 hooks | `pnpm typecheck` | 同上 |
| 写测试 | `node backend/tests/xxx.test.cjs` | 读测试输出→改测试或代码 |
| 加路由 | `pnpm typecheck` | 同上 |
| 全量检查 | `pnpm typecheck && pnpm lint && pnpm test` | 逐个修 |

---

## 自主执行流程（Memory 闭环）

### 0. 必读

```
cat /Users/liyuzhen/Desktop/TK观察工作台/agents.md
cat /Users/liyuzhen/Desktop/TK观察工作台/docs/后续迭代建议.md
cat /Users/liyuzhen/Desktop/TK观察工作台/docs/模块模板说明.md
```

### 1. Skill 流程（参照达人管理模板复制，不改结构）

```
features/business/blog/
├── components/blog-table.tsx    # 参照 creator-table
├── components/blog-form.tsx     # 参照 creator-form
├── components/blog-detail.tsx   # 参照 creator-detail
├── hooks/use-blogs.ts           # 参照 use-creators
├── hooks/use-blog.ts
├── hooks/use-create-blog.ts
├── hooks/use-update-blog.ts
├── hooks/use-delete-blog.ts
├── types.ts
└── index.ts
```

### 2. vault 项目状态

```
ls /Users/liyuzhen/Desktop/TK观察工作台/backend/pb_migrations/
cat /Users/liyuzhen/Desktop/TK观察工作台/backend/pb_migrations/1785866000_create_business_workspace.js
cat /Users/liyuzhen/Desktop/TK观察工作台/backend/pb_hooks/video_ideas.pb.js
```

### 3. rules 权限规则

新建 `backend/pb_migrations/1786104000_business_blog.js`：

- competitor_accounts 权限扩展给 business + seed 3 条对标账号（霞光社/白鲸出海/晚点财经）
- 新建 blog_articles 表（字段见下方）
- clients 表 industry 枚举追加 ai_tool/creator_tool/erp/payment/finance_tax

**blog_articles 表：**
| 字段 | 类型 | 说明 |
|---|---|---|
| title | text(200) | 文章标题 |
| account | select | TK观察 / 对标账号名 |
| publish_date | date | 发布日期 |
| views | number | 阅读量 |
| likes | number | 在看数 |
| shares | number | 转发数 |
| is_viral | bool | 自动计算（views ≥ 同账号均值2倍） |
| analysis_notes | text | 分析笔记 |
| source_url | url | 原文链接 |

### 4. hooks 校验

`backend/pb_hooks/blog_articles.pb.js`：参照 `video_ideas.pb.js`，onRecordCreate/onRecordUpdate 时自动算 is_viral。

### 5. eval 测试

`backend/tests/blog-engine.test.cjs`（≥3个）：is_viral边界值、列表筛选、并发去重。
跑通前不允许 commit。

### 6. 前端

- `features/business/blog/` 全套
- `routes/_app/business/index.tsx` 追加第 9 Tab "公众号分析"
- 不改已有 8 个 Tab 的任何代码

### 7. 完成标准

```
pnpm typecheck → 零错误
pnpm lint → 零警告
pnpm test → 全部通过
node backend/tests/blog-engine.test.cjs → 通过
```

全过才能 commit。

### 8. 总结报告

写 `docs/2026-08-08-business-blog-summarize.md`：
- 改动文件列表
- 测试结果
- 遇到的问题和修复方式
- 后续建议

如有阻塞项写 `docs/2026-08-08-blockers.md`。

---

## 执行清单

```
□ Step 0: 读 agents.md + docs/
□ Step 1: 读 creator 模板 + 1785866000 + video_ideas.pb.js
□ Step 2: 建 migration 1786104000 → node -c 验证语法
□ Step 3: 建 blog_articles.pb.js → 语法验证
□ Step 4: 建 blog-engine.test.cjs → node 跑通
□ Step 5: 建 features/business/blog/ → pnpm typecheck
□ Step 6: 加路由 Tab → pnpm typecheck
□ Step 7: pnpm typecheck && pnpm lint && pnpm test → 全过
□ Step 8: git commit
□ Step 9: 写总结报告
```
